import React from "react";
import Layout from "@/components/layout";
const contact = () => {
  return (
    <React.Fragment>
      <Layout></Layout>
    </React.Fragment>
  );
};

export default contact;
